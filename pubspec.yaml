name: bazar_sales
description: A sales management mobile app for managing products, coffee sales, and donations
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  intl: ^0.19.0
  supabase_flutter: ^2.5.7
  fl_chart: ^0.66.0
  table_calendar: ^3.0.9
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  image_cropper: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
